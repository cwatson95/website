# QF-03 — Gauge Theories — QED & Gauge Invariance

Third module of the **QUANTUM FIELD THEORY** trunk (see `modules/topic_network.txt`).
This is the field-theory end of **KEY BRIDGE B8**: *symmetry → conservation →
interaction* (`~MA-18` Lie groups → `~CM-18` Noether → QF-03 gauge).

- **Prerequisites:** `~QF-01` (the quantized Dirac & EM fields this Lagrangian is
  built from), `~QM-22` (the Dirac equation $i\gamma^\mu\partial_\mu\psi=m\psi$),
  `~CM-18` (Noether: a global symmetry gives a conserved current).
- **Cross-links:** `~EM-09` (the classical abelian gauge freedom **A** → **A** + ∇λ —
  the seed of all this), `~MA-18` (U(1)/Lie groups), `~EM-18` (the field tensor
  $F^{\mu\nu}$), `~QM-04` (the minimal-coupling gauge-invariant current), `~EM-01`
  (the Coulomb $1/r$ the massless photon produces), `~QF-04` (renormalization, which
  gauge symmetry protects).

## Scope
Quantum electrodynamics is *derived from a symmetry demand*, not assumed. The free
Dirac Lagrangian has a **global** U(1) phase symmetry ψ → e^{iα}ψ whose Noether
charge (`~CM-18`) is the electric current j^μ = ψ̄γ^μψ. Promote that phase to a
**local** one, α → α(x), and the derivative ∂_μψ picks up a stray i(∂_μα)ψ that
breaks the symmetry. Restoring it **forces** a connection field A_μ, coupled through
the **covariant derivative** D_μ = ∂_μ + ieA_μ (**minimal coupling**), with the
paired gauge transformation A_μ → A_μ − (1/e)∂_μα making D_μψ → e^{iα}D_μψ transform
covariantly. The antisymmetric **field strength** F_μν = ∂_μA_ν − ∂_νA_μ is then
**gauge invariant** (numerically to machine precision), and the **QED Lagrangian**
ℒ = ψ̄(iγ^μD_μ − m)ψ − ¼F_μνF^μν follows uniquely — the interaction −e j^μ A_μ
*generated by the symmetry*. Three consequences close the module: gauge invariance
**forbids a photon mass** (½m²A_μA^μ is not invariant), so the photon is exactly
massless; the **Ward identity** k_μℳ^μ = 0 makes only the two transverse photon
polarizations physical; and the massless photon's propagator Fourier-transforms to
the long-range **Coulomb** potential 1/(4πr) — the m → 0 limit of the short-range
Yukawa e^{−mr}/(4πr). SI/natural units, Peskin conventions.

## Operations — `code/gauge_theory.py`

| call | meaning | reference |
|------|---------|-----------|
| `field_strength(A, dx)` | F_μν = ∂_μA_ν − ∂_νA_μ (gauge invariant; `[D_μ,D_ν]=ieF_μν`) | Pe §4.1 |
| `gauge_transform(A, lambda_field, dx)` | A_μ → A_μ + ∂_μλ (leaves F_μν unchanged) | Pe §4.1; cf. `~EM-09` |
| `covariant_derivative(psi, A, e, dx)` | D_μψ = ∂_μψ + ieA_μψ (minimal coupling) | Pe §4.1 |
| `yukawa_to_coulomb(r, m)` | V = e^{−mr}/(4πr) → 1/(4πr) as m → 0 (photon propagator) | Pe §4.7 |

The gauge field `A` is a lattice array of shape `(D,) + grid_shape` (`A[mu]` is the
μ-component); the matter field `psi` is a complex array of shape `grid_shape`; `e` is
the coupling and `dx` the lattice spacing.

## Use
```python
import numpy as np
from gauge_theory import field_strength, gauge_transform, covariant_derivative, yukawa_to_coulomb

N, L = 64, 2*np.pi; dx = L/N
x = np.linspace(0, L, N, endpoint=False); X0, X1 = np.meshgrid(x, x, indexing="ij")
A = np.stack([0.4*np.sin(X1), 0.3*np.cos(X0)])         # a gauge potential A_mu(x)

F0 = field_strength(A, dx)                              # F_mu_nu = d_mu A_nu - d_nu A_mu
lam = np.cos(X0)*np.cos(X1)                             # any scalar gauge function
np.max(np.abs(field_strength(gauge_transform(A, lam, dx), dx) - F0))  # ~2e-15: F is gauge invariant

yukawa_to_coulomb(1.0, 0.0)                             # 1/(4*pi) = 0.0796  (Coulomb = massless limit)
```

## Run
```bash
cd code
python3 gauge_theory.py        # demo: F gauge-invariance residual ~2e-15, |D psi| vs |d psi|, Yukawa->Coulomb
python3 test_gauge_theory.py   # tests  ->  "All 9 tests passed."
```

## Files
- `notes.md` — the gauge principle derived step by step (10 display equations)
- `code/gauge_theory.py`, `code/test_gauge_theory.py`
- `problems/problems.md` — worked problems (Peskin Ch. 4, 5, 7; Zee §III.4)
- `refs.md` — citation table (Peskin primary, Zee cross-cite; section-level granularity)
